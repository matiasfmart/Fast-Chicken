@model Combo

<div class="col-4 mb-3">
    <button class='btn-menu btn btn-outline-primary shadow-sm rounded' type="button">
        <div class="container">
            <h5 class='title-menu'>@Model.Name</h5>
            <div class="row">
                <div class="col-12">
                    <h6 class='text-muted'><small>@Model.Description</small></h6>
                </div>
            </div>
        </div>
    </button>
</div>